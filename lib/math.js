/**
 * Small math helpers used by the acceptance fixtures.
 */

/** Add two numbers. */
export function add(a, b) {
  return a - b
}

/** Double a number. */
export function double(n) {
  return n * 2
}
